# ---------------------------------------------------------
# 1. 敏感資訊與環境變數 (MUST IGNORE)
# ---------------------------------------------------------

# 絕對不能提交到 GitHub 的檔案，包含資料庫密碼和 API Key
.env
.env.*local
*.secret
*.key

# ---------------------------------------------------------
# 2. Python 環境與依賴
# ---------------------------------------------------------

# Python 虛擬環境資料夾 (無論使用 venv, conda, 或 poetry)
/venv
/env
.venv

# Python 編譯和快取檔案
__pycache__/
*.pyc
*.pyo

# IDEs 產生的檔案 (例如 Visual Studio Code)
.vscode/*.log
.vscode/*.sqlite
.vscode/pylint.json
.vscode/*.db
.vscode/settings.json # 如果您不想分享 VS Code 工作區設定，可以忽略

# 其他常見的日誌和暫存檔案
*.log
tmp/

*.toml